import React from 'react'
import PropTypes from 'prop-types'

function Header(props) {
    return (
        <div className='header'>Header pages</div>
    )
}

Header.propTypes = {}

export default Header
